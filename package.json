{
  "name": "foundry-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "scrape": "npm run scrape --prefix backend",
    "scrape:deep": "npm run scrape:deep --prefix backend",
    "scrape:clean": "npm run scrape:clean --prefix backend",
    "scrape:playwright": "npm run scrape:playwright --prefix backend"
  }
}
